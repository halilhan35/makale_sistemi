package com.makale_sistemi.repository;

import com.makale_sistemi.entity.MakaleLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MakaleLogRepository extends JpaRepository<MakaleLog, Long> {

}